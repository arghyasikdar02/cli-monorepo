# Pre-Migration Legacy Project

This folder preserves the previous Cyber Lab IN implementation for audit and recovery:

- `cyberscout/`: Vite React frontend.
- `cyberscout-server/`: Express auth backend.

The production working application now lives at the repository root using the required monorepo architecture:

- `apps/`
- `services/`
- `packages/`
- `prisma/`
- `infra/`
- `docs/`

Do not deploy this legacy folder. Useful assets and copy have been migrated into the new platform.

