import { EmptyState, Metric, Shell } from '@cyberlabin/ui';
async function load(){ try { const res = await fetch((process.env.NEXT_PUBLIC_API_BASE_URL || 'http://localhost:3000') + '/api/dashboard/admin', { cache: 'no-store', headers: { 'x-user-id': process.env.DEV_USER_ID || '00000000-0000-0000-0000-000000000001', 'x-roles': process.env.DEV_ROLES || 'admin,instructor,student,sales_agent' } }); if(!res.ok) return null; return res.json(); } catch { return null; } }
export default async function Page(){ const data = await load(); const metrics = data?.metrics || []; return <Shell><div className="mx-auto max-w-6xl px-6 py-10"><p className="text-sm font-semibold text-sky-600">Cyber Lab IN</p><h1 className="mt-3 text-4xl font-semibold tracking-tight">Admin Dashboard</h1><p className="mt-3 max-w-2xl text-slate-600 dark:text-slate-300">Platform analytics, revenue, audit logs, live class health, protected document access, and system visibility.</p><div className="mt-8 grid gap-4 md:grid-cols-3">{metrics.map((m:any)=><Metric key={m.label} {...m}/>)}</div><div className="mt-8"><EmptyState title={data ? 'Database-backed view ready' : 'Connect API to view live data'} body={data ? 'Metrics are loaded from Cyber Lab IN APIs and will show zeroes when the database is empty.' : 'Set NEXT_PUBLIC_API_BASE_URL to the web app API and start the web app.'}/></div></div></Shell> }