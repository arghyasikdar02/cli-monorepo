import './globals.css'; import type { ReactNode } from 'react'; import type { Metadata } from 'next'; export const metadata: Metadata = { title: 'Admin Dashboard | Cyber Lab IN', description: 'Platform analytics, revenue, audit logs, live class health, protected document access, and system visibility.' }; export default function RootLayout({children}:{children:ReactNode}){return <html lang="en"><body>{children}</body></html>}