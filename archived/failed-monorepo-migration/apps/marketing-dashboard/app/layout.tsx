import './globals.css'; import type { ReactNode } from 'react'; import type { Metadata } from 'next'; export const metadata: Metadata = { title: 'Sales and Marketing Dashboard | Cyber Lab IN', description: 'Lead capture, source attribution, follow-ups, conversion funnel, and rule-based sales insights.' }; export default function RootLayout({children}:{children:ReactNode}){return <html lang="en"><body>{children}</body></html>}