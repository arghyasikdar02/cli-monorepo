import './globals.css'; import type { ReactNode } from 'react'; import type { Metadata } from 'next'; export const metadata: Metadata = { title: 'Student Dashboard | Cyber Lab IN', description: 'Course-isolated learning, live classes, labs, quizzes, protected documents, progress, certificates, and AI chat history.' }; export default function RootLayout({children}:{children:ReactNode}){return <html lang="en"><body>{children}</body></html>}