import './globals.css'; import type { ReactNode } from 'react'; import type { Metadata } from 'next'; export const metadata: Metadata = { title: 'Instructor Dashboard | Cyber Lab IN', description: 'Assigned-course teaching operations, attendance, weak learner signals, labs, assessments, and engagement monitoring.' }; export default function RootLayout({children}:{children:ReactNode}){return <html lang="en"><body>{children}</body></html>}